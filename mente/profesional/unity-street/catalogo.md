# UNITY STREET — Catálogo

Fuente de verdad del catálogo: `assets/images/product-*.{jpg,png}` +
markup en `index.html`. Este archivo es para notas sobre el catálogo, no
para duplicar el inventario.

## Inventario actual (por imágenes presentes)

13 productos (`product-1` a `product-13`), sin nombre, precio ni
categoría documentados fuera del HTML.

## Pendientes

- [ ] Documentar cada producto: nombre, precio, categoría, stock.
- [ ] Decidir si el catálogo vive solo en HTML estático o pasa a un
      formato editable (JSON/CSV) que alimente la página.
